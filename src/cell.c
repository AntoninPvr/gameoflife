#include "cell.h"
#include "board.h"

uint8_t neighborhood(uint32_t index, struct Board *p_board) {
    if(index < p_board->column*p_board->row) {
        uint8_t sum_neighborhood = 0;
    }
    else return(9) //9 > 8 invalid value
}